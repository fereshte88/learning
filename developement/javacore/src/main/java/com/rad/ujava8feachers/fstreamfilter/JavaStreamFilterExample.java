package com.rad.ujava8feachers.fstreamfilter;

/**
 * Java stream provides a method filter() to filter stream elements on the basis of given predicate.
 * Suppose you want to get only even elements of your list then you can do this easily with the help of filter method.
 * This method takes predicate as an argument and returns a stream of consisting of resulted elements.
 * جاوا یک متد ()filter برای فیلتر کردن عناصر جریان بر اساس گزاره داده شده فراهم می کند.
 * فرض کنید می خواهید فقط عناصر لیست خود را بدست آورید ، می توانید با کمک روش فیلتر این کار را به راحتی انجام دهید.
 * این روش گزاره را به عنوان یک استدلال می گیرد و جریان متشکل از عناصر نتیجه را برمی گرداند.
 */

/**
 * Signature
 *
 * The signature of Stream filter() method is given below:
 *
 *     Stream<T> filter(Predicate<? super T> predicate)
 *
 * Parameter
 *
 * predicate: It takes Predicate reference as an argument. Predicate is a functional interface. So, you can also pass lambda expression here.
 * Return
 *
 * It returns a new stream.
 */
public class JavaStreamFilterExample {
}
